/**
 * @deprecated This module was replaced by the provider-agnostic LLM service.
 * Import from "./llm.js" instead. Kept as a re-export shim so any external
 * code importing "services/gemini.js" keeps working.
 */
export * from "./llm.js";
